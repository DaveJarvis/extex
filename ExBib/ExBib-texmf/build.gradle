dependencies {
    implementation project(':ExTeX-resource')
}

description = 'ExBib texmf'

tasks.register('testsJar', Jar) {
    archiveClassifier = 'tests'
    from(sourceSets.test.output)
}

publishing.publications.maven.artifact(testsJar)
