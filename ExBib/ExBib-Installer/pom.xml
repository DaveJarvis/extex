<project xmlns="http://maven.apache.org/POM/4.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.extex</groupId>
  <artifactId>ExBib-Installer</artifactId>
  <name>ExBib Installer</name>
  <description>
    This is the ExBib install system. It creates a GUI installer for ExBib and
    related packages in the Maven `package' phase. 
  </description>
  <version>0.2</version>
  <packaging>pom</packaging>
  <inceptionYear>2008</inceptionYear>
  <url>http://www.extex.org</url>

  <distributionManagement>
    <site>
      <id>org.extex.exbib.installer</id>
      <name>ExBib Installer Site</name>
      <url>file:///serv/www/extex/snapshot/ExBib/Exbib-Installer</url>
    </site>
  </distributionManagement>

  <parent>
    <groupId>org.extex</groupId>
    <artifactId>ExBib</artifactId>
    <version>0.2</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <dependencies>

    <dependency>
      <groupId>org.extex</groupId>
      <artifactId>ExBib-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.extex</groupId>
      <artifactId>ExBib-Main</artifactId>
    </dependency>

    <dependency>
      <groupId>org.extex</groupId>
      <artifactId>ExBib-Util</artifactId>
      <version>0.1</version>
    </dependency>

    <dependency>
      <groupId>org.extex</groupId>
      <artifactId>ExBib-ant</artifactId>
      <version>0.1</version>
    </dependency>

    <dependency>
      <groupId>org.extex</groupId>
      <artifactId>ExBib-bsf</artifactId>
      <version>0.1</version>
    </dependency>

    <dependency>
      <groupId>org.extex</groupId>
      <artifactId>ExBib-groovy</artifactId>
      <version>0.1</version>
    </dependency>

    <dependency>
      <groupId>org.extex</groupId>
      <artifactId>ExBib-jython</artifactId>
      <version>0.1</version>
    </dependency>

    <dependency>
      <groupId>org.codehaus.izpack</groupId>
      <artifactId>izpack-standalone-compiler</artifactId>
      <version>4.3.2</version>
      <optional>true</optional>
    </dependency>
  </dependencies>

  <properties>
    <staging.dir>${project.build.directory}/staging</staging.dir>
    <project.build.sourceEncoding>ISO-8859-1</project.build.sourceEncoding>
    <exbib.directory>${basedir}/..</exbib.directory>
  </properties>

  <modules>
    <module>..</module>
    <module>../ExBib-doc</module>
  </modules>
  
  <build>
    <defaultGoal>package</defaultGoal>

    <plugins>
      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>create-staging-area</id>
            <phase>process-resources</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <tasks>
                <copy todir="${staging.dir}"
                      encoding="ISO-8859-1">
                  <fileset dir="${basedir}/src/main/izpack" />
                </copy>
              </tasks>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.codehaus.izpack</groupId>
        <artifactId>izpack-maven-plugin</artifactId>
<!-- 
	<version>5.0.0-beta7</version>
 -->
        <version>1.0-alpha-5</version>
        <configuration>
          <izpackBasedir>${staging.dir}</izpackBasedir>
          <kind>standard</kind>
          <output>${project.build.directory}/${project.artifactId}-${project.version}.jar</output>
          <installerFile>${project.build.directory}/${pom.artifactId}-${project.version}.jar</installerFile>
          <descriptorEncoding>ISO-8859-1</descriptorEncoding>
        </configuration>
        <executions>
          <execution>
            <id>standard-installer</id>
            <phase>package</phase>
            <goals>
              <goal>izpack</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

  <developers>
    <developer>
      <id>gene</id>
      <name>Gerd Neugebauer</name>
      <email>gene@gerd-neugebauer.de</email>
      <url>http://www.gerd-neugebauer.de</url>
      <roles>
        <role>maintainer</role>
        <role>developer</role>
        <role>build manager</role>
      </roles>
      <timezone>+1</timezone>
      <properties/>
    </developer>
  </developers>

</project>
